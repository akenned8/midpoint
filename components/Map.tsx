// Mapbox GL JS map with marker and isochrone layer support
'use client';

export default function Map() {
  // TODO: Initialize Mapbox GL map, render person markers, draw isochrone polygons
  return <div id="map" className="h-full w-full" />;
}
