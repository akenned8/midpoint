// Fairness ↔ Efficiency slider
'use client';

export default function ObjectiveSlider() {
  // TODO: Slider that adjusts alpha between fairness (minimax) and efficiency (sum)
  return <div>ObjectiveSlider</div>;
}
