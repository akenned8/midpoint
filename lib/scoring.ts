// Objective functions for scoring candidate midpoints

// TODO: Minimize the maximum travel time (fairness)
export function minimax(_times: number[]): number {
  return 0;
}

// TODO: Minimize sum of squared travel times (efficiency)
export function sumOfSquares(_times: number[]): number {
  return 0;
}

// TODO: Blend fairness and efficiency using alpha parameter
export function blended(_times: number[], _alpha: number): number {
  return 0;
}

// TODO: Detect if one person's travel time is a statistical outlier
export function detectOutlier(_times: number[]): number | null {
  return null;
}
