// Shared link entry point — reads URL state and renders results

export default function SharedPage() {
  // TODO: Read lz-string encoded state from URL search params
  // TODO: Decode SessionState and render results view
  return (
    <main>
      <p>Loading shared session...</p>
    </main>
  );
}
